import React from 'react'

/**
 * Open ended input for users to add their own identities to analyze with the
 * selected legislation.
 * 
 * @param {*} submitDemographics Reference to function that submits the demographics 
 * to python backend so that ai response can be generated.
 */
const SelectDemographics = ({SubmitDemographics}) => {
  return (
    <div>SelectDemographics</div>
  )
}

export default SelectDemographics